# PROJECT_ARCHITECTURE.md

> - **v11:** Реализована и подтверждена end-to-end Стадия 2 (обучение). Модель-агностичный `src/train.py` (тонкий YAML-раннер `python -m src.train --config …`, §4), ленивый `src/data/dataset.py` (кэш → батч; фикс-длина кропом/повтор-паддингом, рандом-кроп на обучении), детерминированные подвыборки `src/data/subsets.py` (§6.4), реестр моделей `src/models/` (+ baseline-НАПОЛНЕНИЕ `statpool_mlp`, §1), общий EER `src/utils/metrics.py` (переиспользуется Стадией 3). Pre-flight сверка `C` (§6.6), чекпойнты с ротацией + атомарной записью, `metrics.jsonl` (append + дедуп при возобновлении), val-EER по эпохам, `pos_weight` из дисбаланса. `pyyaml` → обязательная зависимость. Проверено end-to-end (обучение/возобновление/pre-flight/ротация/детерминизм от seed) на синтетике + юнит-тесты; smoke на реальном `lfcc`-кэше (train/dev 2019 LA) отработал. **Сознательно отложено** (§7, §9): нормализация входа, LR-scheduler, реальное маскирование переменной длины, выбор настоящей архитектуры, полные прогоны.
> **Назначение этого файла.** Это «паспорт» проекта для ассистента (Claude). Цель —
> чтобы в начале любого нового чата по одному прочтению этого файла можно было
> восстановить: что за проект, какие ограничения, как устроена архитектура, за что
> отвечает каждая папка/файл, какие есть стадии, какие приняты конвенции и что ещё
> **не решено**. Файл самодостаточен: для понимания не нужна история переписки.
>
> **Статусы по тексту:** `[РЕШЕНО]` — зафиксировано; `[ОТКРЫТО]` — ожидает решения
> пользователя; `[НЕ НАЧАТО]` — запланировано, кода ещё нет.
>
> **Дисциплина утверждений.** *Факт* — проверяемое свойство инструментов/данных;
> *решение* — принятая конвенция (могла быть иной); *оценка* — вывод с явной степенью
> уверенности. Там, где утверждение не факт, это помечается в тексте.

---

## 1. О проекте

- **Тема (магистерская работа):** Разработка метода выявления синтетической речи в аудио.
- **Суть задачи:** на основе обзора существующих работ найти, что можно улучшить, и реализовать это. Улучшение не обязано быть SOTA — достаточно нового ракурса с обоснованием.
- **Критерии защиты:**
  1. *Актуальность и научная новизна* — выводятся из обзора литературы (достаточно небольшого улучшения или нового подхода).
  2. *Доказуемость и измеримость* — чёткая структура, обоснованные шаги, каждый вывод подкреплён теоремой / научной работой / экспериментом.
- **Стадия работы:** практическая часть начата; основное направление **ещё не выбрано** — идут эксперименты с потенциальными направлениями.

### Связка «лёгкость метода → новизна» (важно помнить)
Новизна проекта — в том, что **метод лёгкий**: дёшев в развёртывании и инференсе на ограниченных ресурсах. По обзору литературы проекта SOTA достигается ценой SSL-моделей ~316M параметров, требующих GPU, и авторы сами признают это ограничением применимости. Поэтому «лёгкий метод под ограниченные ресурсы» — защитимый аргумент актуальности.

**Разводить две вещи** (иначе теряется аргумент новизны):
- *На чём метод обучается* — **переменная**. Сейчас это CPU-ноутбук; маловероятно, но возможно предоставление GPU-машины. Железо разработки к научной ценности отношения не имеет.
- *Какой у метода вес* — **это и есть вклад**. Ценность лёгкости — в дешёвом *развёртывании/инференсе*, а не в том, на каком железе шло обучение.
- **Риск при появлении GPU** (не архитектурный, а дисциплинарный): соблазн взять тяжёлый SSL «потому что теперь можно» — и тогда лёгкость как ракурс новизны теряется. Даже с GPU итоговый метод держать лёгким. Формулировать защиту научно («лёгкие методы важны для реального развёртывания»), а не «у меня слабый ноутбук».

---

## 2. Жёсткие ограничения `[РЕШЕНО]`

- **Язык:** Python 3.12.3.
- **Обязательные библиотеки:** `numpy`, `librosa`, `pytorch`, `pandas`, `matplotlib`, `sklearn`, `pyarrow` — движок чтения/записи `.parquet` (формат манифестов и выгрузки скоров, §4–§6; без него `pandas.to_parquet`/`read_parquet` не работают), `pyyaml` — чтение конфигов Стадий 2–3 (`experiments/<exp>.yaml`; **подтверждено на Стадии 2, v11**). Остальные — по необходимости (`tqdm` для прогресса, `psutil` для контроля памяти).
- **Декодирование аудио / фиксация версии `soundfile` `[РЕШЕНО]`.** `librosa` читает аудио через `soundfile` (libsndfile). Зафиксировано `soundfile==0.12.1` (несёт libsndfile **1.2.0**). Причина — *факт, проверенный на данных*: libsndfile **1.2.2** не декодирует часть валидных FLAC ASVspoof 2021 (ошибка его FLAC-декодера «lost sync» / «unknown error»; при этом заголовок `sf.info` читается — что и путало диагностику), а 1.2.0 эти файлы декодирует. Любой `pip install -U soundfile` вернёт баг — **не обновлять** без перепроверки; это важно для воспроизводимости среды (критерий №2). Как страховку Стадия 1 умеет фолбэк на `ffmpeg` (независимый декодер, включается автоматически при сбое soundfile), но при 0.12.1 он на текущих датасетах не задействуется.
- **Возобновляемость обязательна.** Прогоны на CPU длительные; стадия, прерванная на N%, при рестарте продолжает с невыполненного, а не с нуля (§7). Это требование не зависит от машины и полезно и на GPU.

**Текущий профиль машины (не аксиома архитектуры).** Разработка идёт на ноутбуке **без GPU, на CPU**, с бюджетом RAM **≤ 5 ГБ** (всего 7.7 ГБ). Это *свойство текущей машины*, а не встроенное в архитектуру допущение: `device` (`cpu`/`cuda`), `batch_size`, `num_workers` — **параметры конфига, а не константы в коде** (§3, §7). Возможный переезд на GPU-машину при этом не требует переписывания.

---

## 3. Архитектурная философия (почему всё устроено именно так)

**Принцип, не зависящий от машины:** пиковое потребление памяти должно зависеть от **размера батча, а не от размера датасета.** Это верно для любой конечной памяти — RAM на CPU сейчас, VRAM на GPU потом.

Отсюда — **долговечное ядро** (полезно на любом железе и обслуживает критерий №2):

1. **Стадийность с кэшем на диск.** Обработка разнесена на независимые стадии; промежуточные результаты лежат на диске, в памяти держится только текущая порция.
2. **Признаки считаются один раз и кэшируются.** Извлечение признаков — самый дорогой шаг на CPU; платится один раз, дальше эксперименты читают готовый кэш. Кэш ещё и **переносим между машинами**.
3. **Возобновляемость (resumability).** Длинная стадия при перезапуске пропускает уже сделанное (§7).
4. **Развязанность = сравнимость экспериментов.** Смена «направления» — замена одного модуля (признаки или модель) без переписывания остального. Каждая стадия логируется отдельно → измеримость.
5. **Агностичность по трём осям `[РЕШЕНО как требование]`.** Датасеты, признаки и модели подбираются экспериментально → система не «знает» заранее конкретный датасет/признак/модель. Стабильные интерфейсы между сменными частями: **манифест** (любой датасет → пайплайн, §6.1), **кэш признаков** (любые признаки → обучение, §6.5) и **контракт признак↔модель** (любые признаки → любая модель, §6.6).
   - **Правило не-ветвления (простое операциональное определение).** Ниже манифеста стадии читают только канонические колонки (§6.1). Единственные законные употребления `dataset_id` — `concat` (склейка манифестов) и `group-by` (стратификация/разбивка). Появление `if dataset_id == …` ниже манифеста — **code smell**: чинить надо схему, а не вставлять частный случай. Всё датасет-специфичное живёт **только** в адаптере `sources/` (§6.2).
   - **Глобальный ключ примера — пара (`dataset_id`, `utt_id`).** Имена файлов между датасетами могут совпадать, поэтому `utt_id` глобально не уникален. Пара уникальна и служит ключом кэша признаков и ключом «пропустить готовое» (§7).
6. **Портируемость.** Устройство и размеры батча/воркеров — параметры (§2, §7); цикл обучения делает `tensor.to(device)`, а не хардкодит `.cpu()`. Никакой «мультидевайсной абстракции» не строится — это было бы переусложнением; достаточно не вписывать «CPU» в код как факт.

### Текущий профиль памяти (потолок 5 ГБ) — где реально риск
Это раздел про *текущую машину*, не про архитектуру. Из 5 ГБ часть уже занимает базовый процесс: импорт `torch`/`numpy`/`librosa` резидентно — обычно сотни МБ (*оценка, не точный факт — стоит измерить через `psutil`*). Реальный бюджет под данные/модель ≈ 3.5–4 ГБ. Риск OOM концентрируется **не** в стадии 1:
- **Стадия 1 безопасна по RAM.** Один аудиофайл во `float32` — единицы–десятки МБ (5 мин при 16 кГц ≈ 18 МБ). Стадия 1 ограничена временем CPU и местом на диске, а не памятью.
- **Реальные источники OOM:** (а) случайная материализация всего датасета/матрицы признаков в один массив/`DataFrame`; (б) `DataLoader` с несколькими воркерами — каждый процесс дублирует память; (в) большой батч / удержание графа в обучении; (г) **загрузка тяжёлой модели**: SSL вроде wav2vec2/HuBERT ≈ 300M параметров ≈ **1.2 ГБ только веса** = четверть бюджета, и медленно на CPU → на текущей машине SSL-направление маргинально (учитывать при выборе направления; на GPU это ограничение снимается, но см. дисциплинарный риск в §1).

---

## 4. Стадии пайплайна `[РЕШЕНО]` (структура; наполнение зависит от §9)

| Стадия | Модуль | Что делает | Читает | Пишет (артефакт) |
|---|---|---|---|---|
| 0. Индексация | `src/data/manifest.py` | Один проход по датасету, собирает метаданные. **Аудио не загружает.** | сырое аудио (только пути/метаданные) | манифест `.parquet` |
| 1. Признаки | `src/data/features.py` | Файл за файлом извлекает признаки, сразу пишет на диск. Самый дорогой шаг, возобновляемый. | манифест | кэш признаков (per-file `.npy`) |
| 2. Обучение | `src/train.py` | Обучение модели; ленивая подгрузка признаков через `Dataset`/`DataLoader`. **В памяти только батч.** | кэш признаков | лучшие/финальные веса (`best.pt`/`final.pt`) + **периодические чекпойнты** с ротацией (для возобновления) + построчный лог метрик по эпохам (`metrics.jsonl`) |
| 3. Оценка | `src/evaluate.py` | Инференс на тесте, метрики. Возобновляема (пропуск уже оценённых). | веса + кэш признаков | выгрузка скоров `.parquet` |

> Каждая стадия запускается **независимо**, по контракту `run(config)`.
> **Единой точки входа (CLI/`main.py`) сознательно нет** `[РЕШЕНО]` — стадии гоняются по отдельности (стадия 1 один раз и дорого; стадии 2–3 — многократно). При желании можно добавить тонкий диспетчер позже, но это не требуется.

### Запуск Стадии 0 `[РЕШЕНО]`

Стадия 0 запускается **как модуль пакета** (`python -m …`), а не как файл: в
`manifest.py` относительные импорты (`from ..config …`), поэтому `python
src/data/manifest.py` сломается — правильно только `python -m src.data.manifest`.
Команду выполнять **из корня проекта** (папка, где лежит `src/`). Это *тонкий
раннер одной стадии*, а не глобальный `main.py`/диспетчер (последний §4 сознательно
отвергает); у каждой стадии свой запуск через `-m`.

```bash
# один датасет
python -m src.data.manifest for-2seconds

# несколько сразу
python -m src.data.manifest asvspoof2019_la asvspoof2021_la asvspoof2021_df

# все зарегистрированные адаптеры
python -m src.data.manifest --all

# показать доступные dataset_id и выйти
python -m src.data.manifest --list
```

**Флаги:**

| Аргумент | Назначение |
|---|---|
| `datasets` (позиционные) | один или несколько `dataset_id` (= имена папок в `data/raw/`, совпадают с выводом `--list`) |
| `--all` | обработать все зарегистрированные адаптеры |
| `--list` | показать доступные `dataset_id` и выйти |
| `--root PATH` | задать корень проекта вручную (по умолчанию определяется от расположения `src/config.py`; нужен только при запуске из нестандартного места или на другом дереве данных) |

**Что делает.** Для каждого датасета: адаптер → валидатор (§6.1) → атомарная
запись `data/manifests/<dataset_id>.parquet`; печатает сводку (число строк,
разбивка `split × label`, заполненность `attack_type`/`speaker_id`) — это сразу
санити-проверка. Каталог `data/manifests/` создаётся автоматически.

**Поведение при ошибке.** Один датасет — исключение пробрасывается с понятным
сообщением. Несколько / `--all` — раннер не падает на первом сбое: печатает
`[<ds>] ОШИБКА: …`, продолжает остальные и в конце возвращает код `1` со списком
не удавшихся. Так один битый датасет не прячет успех прочих.

**Тонкости и порядок прогона.**
- Первый прогон делать **по одному датасету** — чтобы изолировать возможный сбой (внешние ключи 2021, наличие меток в eval-протоколе 2019 и т.п.).
- 2021 LA/DF — сотни тысяч файлов: `glob` по папке занимает заметное время, но **аудио не читается** (только пути/метаданные, §4), память не растёт.
- DF даёт **меньше** строк полного eval — это индексируется часть `part00` (Приложение А), так и отчитывается в работе.
- 2019 LA индексируется **по CM-протоколу**; ASV-enrollment из папок `dev/eval` flac (файлы вида `LA_D_A…`) в манифест не входит.

### Запуск Стадии 1 `[РЕШЕНО]`

Как и Стадия 0 — **модуль пакета** (`python -m src.data.features`), из корня
проекта, относительные импорты (`python src/data/features.py` сломается). Тонкий
раннер одной стадии, не глобальный диспетчер (§4). У Стадии 1 две оси: датасет(ы)
и **один экстрактор за прогон** (одна конфигурация признаков = одна папка кэша).

```bash
# один датасет, один экстрактор
python -m src.data.features asvspoof2019_la -f lfcc

# несколько датасетов сразу (экстрактор один на прогон)
python -m src.data.features asvspoof2021_la asvspoof2021_df -f lfcc

# все датасеты, у которых уже построен манифест
python -m src.data.features --all -f logmel

# показать экстракторы и доступные датасеты, выйти
python -m src.data.features --list

# быстрый smoke-тест на первых N примерах
python -m src.data.features for-2seconds -f lfcc --limit 200
```

**Флаги:**

| Аргумент | Назначение |
|---|---|
| `datasets` (позиционные) | один/несколько `dataset_id`; манифест датасета должен существовать |
| `-f`, `--features NAME` | какой экстрактор (список — в `--list`); один на прогон |
| `--all` | все датасеты с готовым манифестом в `data/manifests/` |
| `--list` | показать экстракторы (с `C`/`sr`/хэшем) и доступные датасеты, выйти |
| `--limit N` | только первые N (стабильно отсортированных) примеров — smoke-тест, **не** для итоговых метрик |
| `--root PATH` | корень проекта вручную (по умолчанию — автоопределение от `src/config.py`) |

**Экстракторы (на данный момент).** `lfcc` (C=60, LFCC-60: 20 коэф. × static/Δ/ΔΔ),
`logmel` (C=80). Новый признак = новый класс + `@register_extractor`, ничего ниже
не трогая (реестр — зеркало `sources/`).

**Что делает.** Для каждого датасета читает `data/manifests/<dataset_id>.parquet`
(только метаданные — память не зависит от размера аудио), стабильно сортирует по
(`dataset_id`, `utt_id`), файл за файлом: путь выхода детерминирован → есть →
пропуск; иначе декодирует аудио → извлекает `[T, C]` → проверяет контракт (§6.6) →
**атомарно** пишет `.npy`. Раскладка кэша:
`data/features/<name>-<hash>/<dataset_id>/<utt_id>.npy`, где `hash` = sha1 конфига
признаков. Пишет паспорт папки `_spec.json` (параметры + версии библиотек) и, при
сбоях, `<dataset_id>/_failures.csv` (сбойный файл + реальная причина). Печатает
сводку (всего / записано / пропущено / ошибок).

**Предусловие.** Манифест обязан быть построен (Стадия 0). Нет манифеста → понятная
ошибка с подсказкой запустить Стадию 0.

**Возобновляемость.** «Пропустить готовое» + атомарная запись (§7): прерванный
прогон продолжается с невыполненного. Рабочий цикл: `--limit` (быстро) → полный
манифест, который пропускает уже посчитанное.

**Поведение при ошибке.** Единичный битый/недекодируемый файл логируется в
`_failures.csv` и **не** роняет прогон. Систематический сбой (0 успехов на непустом
входе) или отсутствие манифеста помечает датасет как неудавшийся. Несколько
датасетов — раннер не падает на первом: печатает `[<ds>] ОШИБКА: …`, продолжает и в
конце возвращает код `1` со списком не удавшихся (как в Стадии 0).

**Тонкости.**
- Разные параметры экстрактора → разный `hash` → разные папки кэша, наборы не путаются; возврат к прежним параметрам не вызывает пересчёта.
- `logmel` считать только когда понадобится — иначе зря занятые ГБ на диске.
- Декодирование: soundfile (быстрый путь) с автоматическим фолбэком на ffmpeg при сбое (§2); при `soundfile==0.12.1` фолбэк на текущих датасетах не нужен.
- Итоговый EER берётся на **полном** тесте, не на `--limit`-подвыборке (§6.4).

### Запуск Стадии 2 `[РЕШЕНО]`

Как Стадии 0/1 — **модуль пакета** (`python -m src.train`), из корня проекта
(относительные импорты; `python src/train.py` сломается). Тонкий раннер одной
стадии, не глобальный диспетчер (§4). Прогон описывается **одним YAML-конфигом**
`experiments/<exp>.yaml`; `config.py` остаётся статикой (пути, seed, дефолты
среды), YAML переопределяет их секцией `runtime` (§2, §3).

```bash
# показать зарегистрированные признаки и модели, выйти
python -m src.train --list

# запустить прогон по конфигу
python -m src.train --config experiments/smoke_lfcc_statpool.yaml
```

**Флаги:**

| Аргумент | Назначение |
|---|---|
| `--config PATH` | `experiments/<exp>.yaml` — полное описание прогона (обязателен, кроме `--list`) |
| `--list` | показать доступные признаки/модели и выйти |
| `--root PATH` | корень проекта вручную (по умолчанию — автоопределение от `src/config.py`) |

**Структура конфига** (пример — `experiments/smoke_lfcc_statpool.yaml`):
`name` (→ папка артефактов `experiments/<name>/`), `feature.name` (+ опц.
`overrides` гиперпараметров признака — меняют хэш, должны совпасть с папкой кэша),
`model.{name, params}`, `data.{train, val, t_fixed, subset}`,
`train.{epochs, lr, weight_decay, pos_weight, ckpt_every, keep_last}`, `runtime.*`.
`train`/`val` — списки селекторов `{dataset_id, split}`; при нескольких датасетах
манифесты склеиваются (§3). `subset` (опц.) — детерминированная стратифицированная
подвыборка (§6.4); **убрать секцию = полный прогон тем же кодом**.

**Что делает.** Резолвит признак → его `C` и папку кэша; **pre-flight** сверяет
`C` кэша (из `_spec.json`) с `C`, который объявляет модель (§6.6) — рассинхрон
«признак ≠ модель» и отсутствие кэша ловятся **до** обучения, с понятной ошибкой и
подсказкой про Стадию 1. Читает манифесты по селекторам, берёт подвыборку, лениво
грузит признаки (в памяти только батч, §3/§7), учит; в конце каждой эпохи считает
val-EER (общая утилита `utils/metrics`, §8), пишет строку в `metrics.jsonl` и
чекпойнт. Печатает по эпохам loss и val-EER.

**Артефакты** — в `experiments/<name>/` (§6.5): `metrics.jsonl` (эпоха, loss,
val-EER, порог, lr), `ckpt_epoch*.pt` (последние `keep_last`), `best.pt` (мин.
val-EER), `final.pt` (последняя эпоха).

**Предусловия.** (1) Манифест датасета(ов) построен (Стадия 0). (2) Кэш признака
покрывает те примеры, что попадут в подвыборку. **Полный прогон Стадии 1 не
требуется** — Стадия 2 читает `.npy` по ключу (`dataset_id`, `utt_id`) и
отфильтровывает отсутствующие с предупреждением, т.е. работает по фактически
посчитанному кэшу. *Тонкость:* `features --limit N` считает **первые N** после
стабильной сортировки, а подвыборка обучения берёт **стратифицированные по seed**
примеры — они могут не совпасть с первыми N. Надёжный smoke-режим: либо полный кэш
признака (у `lfcc` он есть, §10/Приложение А), либо согласовать охват вручную.

**Возобновляемость (§7).** Прерванный прогон при перезапуске **той же командой**
подхватывает последний чекпойнт (модель + оптимизатор + эпоха), продолжает с
невыполненной эпохи; `metrics.jsonl` дедуплицируется по эпохе. Ротация держит
последние `keep_last` чекпойнтов (клампится к ≥ 1 — без чекпойнта нет resume).

**Smoke-first (§6.4).** Основной режим разработки — smoke на маленькой подвыборке
(пример-конфиг такой по умолчанию: `n_train`/`n_val` малые): за минуты видно, что
цикл/чекпойнты/лог/val-EER живы. Полные прогоны отложены до завершения проекта;
итоговый EER — на полном тесте (§6.4).

---

## 5. Структура каталогов

```
project/
├── data/
│   ├── raw/              # исходное аудио, ТОЛЬКО ЧТЕНИЕ; по ПОДПАПКЕ на датасет, родная структура нетронута
│   │   ├── <dataset_id>/ # напр. asvspoof2019_la/, for-2seconds/ — но множество датасетов ОТКРЫТО
│   │   └── ...
│   ├── manifests/        # индексы (.parquet) по КАНОНИЧЕСКОЙ СХЕМЕ (§6.1)
│   │   ├── <dataset_id>.parquet   # один полный манифест на датасет (склеиваются через concat по dataset_id)
│   │   ├── ...
│   │   └── subsets/      # детерминированные подвыборки = отфильтрованные манифесты (§6.4)
│   └── features/         # КЭШ признаков: per-file .npy; ключ = (dataset_id, utt_id) + хэш конфига признаков (§6.5)
├── src/
│   ├── config.py         # единый конфиг: пути, seed, device/batch_size/num_workers
│   ├── data/
│   │   ├── sources/      # РЕЕСТР адаптеров датасетов: родной формат → канонический манифест
│   │   │   └── ...       # по одному адаптеру на датасет; МНОЖЕСТВО ОТКРЫТО (имена — не фиксированный список)
│   │   ├── schema.py     # каноническая схема манифеста + ВАЛИДАТОР (контракт §6.1–6.2)
│   │   ├── manifest.py   # Стадия 0: сборка манифеста через sources/ (по одному датасету)
│   │   ├── features.py   # Стадия 1: РЕЕСТР экстракторов признаков (общий интерфейс §6.6) → кэш на диск
│   │   ├── subsets.py    # детерминированные стратифицированные подвыборки (манифест → манифест, §6.4)
│   │   └── dataset.py    # Стадия 2: ленивый torch Dataset поверх кэша + фикс-длина (кроп/повтор-паддинг, рандом-кроп на обучении) + коллация (§6.6)
│   ├── models/           # РЕЕСТР архитектур (общий интерфейс §6.6, выбор из конфига)   [РЕШЕНО]
│   │   ├── base.py       # база AntispoofModel + реестр (@register_model / get_model)
│   │   └── statpool.py   # baseline statpool_mlp — НАПОЛНЕНИЕ (§1), не выбор архитектуры
│   ├── train.py          # Стадия 2: обучение (модель-агностичный цикл, pre-flight C, чекпойнты, val-EER, YAML-раннер)
│   ├── evaluate.py       # Стадия 3: оценка; пишет выгрузку скоров   [НЕ НАЧАТО]
│   ├── viz/              # ВСЯ логика построения графиков (импортируется ноутбуками)
│   │   ├── style.py      # общий matplotlib rcParams + save_figure()
│   │   ├── audio.py      # осциллограммы, спектрограммы, карты признаков
│   │   ├── dataset.py    # баланс классов, длительности, состав сплитов
│   │   ├── training.py   # кривые из логов обучения
│   │   └── results.py    # DET/ROC, матрица ошибок, EER по атакам
│   └── utils/            # общие утилиты
│       ├── io.py         # атомарная запись (.parquet/.npy) — общий строительный блок (§7)
│       └── metrics.py    # общий EER (val на Стадии 2, итоговый на Стадии 3, §8)
├── notebooks/            # ТОНКИЕ ноутбуки: только импорт из src/viz + вызов + сохранение
│   ├── 01_audio_features.ipynb
│   ├── 02_dataset.ipynb
│   ├── 03_training.ipynb
│   └── 04_results.ipynb
├── experiments/          # конфиги (YAML) + результаты на каждый запуск
├── reports/
│   └── figures/          # экспортированные PDF/SVG для отчётов и магистерской
└── requirements.txt
```

---

## 6. Контракт данных и артефактов `[РЕШЕНО]`

Ноутбуки и `viz`-модули **ничего не пересчитывают** — они читают готовые артефакты с диска. Стадии обязаны эти артефакты писать. Но прежде артефактов — **контракт данных**: при открытом множестве датасетов объект проектирования не «ASVspoof и FoR», а *схема и правила, которым обязан удовлетворять любой будущий датасет*. ASVspoof/FoR ниже фигурируют только как два крайних случая (протокол с метаданными / ничего, кроме имён папок), на которых контракт проверяется на прочность.

### 6.1 Каноническая схема манифеста `[РЕШЕНО]`
Манифест — **единственный** универсальный интерфейс «любой датасет → пайплайн». Любой адаптер обязан выдавать ровно эту схему; всё ниже манифеста полагается только на неё (правило не-ветвления, §3).

**Обязательные колонки:**
- `dataset_id` (str) — происхождение, напр. `asvspoof2019_la`. Нужна для безопасного склеивания манифестов (`concat`) при кросс-датасетных экспериментах и как часть глобального ключа.
- `utt_id` (str) — уникален **внутри** датасета. Пара (`dataset_id`, `utt_id`) глобально уникальна → ключ кэша признаков и ключ «пропустить готовое».
- `path` (str) — путь к аудио относительно `raw/<dataset_id>/`.
- `label` (int) — **фиксированная конвенция, одна для всех адаптеров: `spoof = 1`, `bonafide = 0`** (положительный класс = детектируемая синтетика). Это *решение*, а не факт, — но оно единое и записано.
- `split` (str) ∈ `{train, dev, test}` — нормализованный (см. 6.3).

**Опциональные колонки (NULL при отсутствии):**
- `attack_type` (str) — напр. `A01…` у датасетов с протоколом; NULL там, где разметки атак нет.
- `speaker_id` (str) — NULL, если датасет дикторов не размечает.
- `sample_rate` (int), `codec` (str: flac/wav/…), `duration` (float, сек).

**Правило отсутствующих полей:** явный **NULL** (pandas NA), **не** магическая строка (`"unknown"`). Причина — отличать «поля нет в датасете» от «поле есть, но значение неизвестно». Потребители (напр. разбивка EER по атакам, §8) обязаны **пропускать** строки с NULL в `attack_type`, а не считать NULL отдельной «атакой».

> **Почему это критерий №2.** Схема + функция-валидатор (`schema.py`), через которую проходит выход *любого* адаптера (обязательные колонки, типы, `label ∈ {0,1}`, `split ∈ {train,dev,test}`), превращают агностичность из лозунга в **проверяемый контракт** — измеримый вклад.

### 6.2 Контракт адаптера (`sources/*`) `[РЕШЕНО]`
Адаптер инкапсулирует **всё** датасет-специфичное. Обязанности: (1) найти аудио в своём `raw/<dataset_id>/`; (2) разрешить **метку** на файл (источник — дело адаптера: протокол / имя папки / шаблон имени); (3) разрешить **родной сплит** либо пометить его отсутствие (§6.3); (4) заполнить опциональные поля где есть, поставить **NULL** где нет. Выход обязан **проходить валидатор**. Наружу торчит только каноническая схема — обратная сторона правила не-ветвления (§3).

### 6.3 Сплиты: родной против назначенного `[частично ОТКРЫТО]`
Система **не предполагает** наличия родного деления — датасет может быть просто набором файлов. Источник сплита бывает двух видов, адаптер обязан их различать:
- **родной сплит** — адаптер читает его из датасета (протокол или структура папок) и нормализует в `{train, dev, test}` (напр. `eval → test`; `validation → dev`);
- **назначенный сплит** — датасет деления не имеет → сплит присваивает **проект** по явной политике-параметру.

**Политика назначения (предпочтение):** деление **по диктору** — чтобы записи одного диктора не попадали и в `train`, и в `test` (утечка диктора, §8). Возможно только при наличии `speaker_id`; иначе деградация до **случайного с seed** + **явная пометка об ограничении**. Дефолт политики — `[ОТКРЫТО]` (§9).

### 6.4 Подвыборки `[РЕШЕНО]`
Подвыборка = **отфильтрованный манифест**, а не копия аудио. Указывает на те же файлы в `raw/` и те же записи в `features/`. Бесплатна по диску. Реализация — `src/data/subsets.py` (v11).
- **Детерминизм (критично).** Перед сэмплированием — стабильная сортировка по (`dataset_id`, `utt_id`), **затем** выборка с зафиксированным seed. Без сортировки тот же seed на иначе упорядоченном `DataFrame` даёт **другие** строки → эксперименты несравнимы. *(Инвариант реализован и проверен: одинаковый результат при разном исходном порядке строк.)*
- **Стратификация.** Минимум по (`split`, `label`); желательно по `attack_type`; при смешивании датасетов — обязательно по `dataset_id` (раннер Стадии 2 добавляет `dataset_id` в стратификацию автоматически при склейке >1 датасета). Разложение бюджета `n` по стратам — метод наибольших остатков (сумма квот = `n`).
- **Артефакт.** `data/manifests/subsets/<dataset_id>__n<N>__seed<S>.parquet` (самодокументирующее имя); параметры (`n`, `seed`, `stratify`) — в `experiments/<exp>.yaml`. *(В цикле обучения подвыборка обычно передаётся прямо в `Dataset`; сохранение на диск — опциональная функция `save_subset`.)*
- **Синергия с кэшем и resumability.** Признаки подвыборки — **строгое подмножество** полных. Рабочий цикл: стадия 1 на подвыборке (быстро) → итерации стадий 2–3 → стадия 1 на полном манифесте, которая **пропускает** уже посчитанные файлы. По-файловый кэш делает это бесплатным.
- **Оговорка (важно для критерия №2).** Подвыборка — для **итерации и отладки**. Итоговый EER берётся на **полном** тесте: на малом тесте оценка EER имеет высокую дисперсию, а порог определён плохо. К итоговым числам желательно приложить **бутстрэп-доверительные интервалы**. *(Бутстрэп-CI для EER — стандартная практика в биометрии; точную ссылку перепроверить перед внесением в текст.)*

### 6.5 Прочие артефакты `[РЕШЕНО]`
- **Кэш признаков** (`data/features/`): **per-file `.npy`** — по одному файлу на пример; ключ пути — **(`dataset_id`, `utt_id`) + хэш конфига признаков**, чтобы разные наборы признаков не путались и не было пересчёта при возврате. → читает `dataset.py` и ноутбук аудиопризнаков. Папка кэша **самоописательна**: `_spec.json` (конфиг признаков + версии библиотек — воспроизводимость) и, при сбоях, `<dataset_id>/_failures.csv` (сбойный файл + причина).
- **Лог метрик обучения** (`experiments/<exp>/metrics.jsonl`): построчно по эпохам — `epoch`, `train_loss`, `val_eer`, `val_threshold`, `lr` (append + **дедуп по эпохе при возобновлении**, §7). → читает ноутбук обучения.
- **Веса и чекпойнты обучения** (`experiments/<exp>/`): `ckpt_epoch*.pt` (модель + оптимизатор + эпоха; последние `keep_last`, для возобновления §7), `best.pt` (мин. val-EER), `final.pt` (последняя эпоха). **Стадия 3 по умолчанию грузит `best.pt`** *(решение; сама Стадия 3 — `[НЕ НАЧАТО]`)*. Оговорка: на малой/шумной val «лучшая эпоха» может оказаться удачей, а не улучшением (высокая дисперсия EER, §6.4) — на полном val это не проблема, на smoke интерпретировать осторожно.
- **Выгрузка скоров** (`experiments/<exp>/scores.parquet`): предсказания/скоры по примерам теста (+ `attack_type`, `dataset_id`). → читает ноутбук результатов. *(Пишет Стадия 3, `[НЕ НАЧАТО]`.)*

### 6.6 Контракт признак↔модель `[РЕШЕНО]` (модули реализованы, Стадия 2)
Третий интерфейс агностичности. «Модель-агностичный цикл обучения» остаётся агностичным только пока форма того, что кладёт экстрактор, зафиксирована — иначе LFCC `[T, 60]`, F0 `[T, 1]` и SSL-эмбеддинг ломают произвольную модель.

- **Форма.** Каждый экстрактор отдаёт `float32`-массив **`[T, C]`** (`T` — кадры по времени, `C` — каналы/коэффициенты). 1D-признаки (напр. F0) — это `[T, 1]`. Кэш (§6.5) хранит ровно `[T, C]`.
- **Коллация.** `dataset.py` возвращает один пример `[T, C]`; коллатор собирает батч `[B, T_fixed, C]` + `lengths`. *На Стадии 2 длина фиксируется (`t_fixed`), поэтому маска тривиальна (все `T` равны), но `lengths` возвращается — интерфейс сохранён под будущую честную переменную длину без правки моделей.* Это единственное место, где сходятся корректность и память батча.
- **Объявление входа.** Модель из реестра `models/` объявляет ожидаемое `C` (`in_channels`); несовпадение с конфигом признаков — ошибка конфигурации, ловится **до** обучения (как валидатор схемы в §6.1). *Реализовано: pre-flight в `train.py` сверяет `C` кэша (из `_spec.json`) с `C` модели.* Пулинг/свёртка по `T` до фиксированной длины — **обязанность модели**, не кэша: кэш остаётся `[T, C]`, контракт стабилен.
- **Устройство.** Перенос на `device` — здесь: коллатор/цикл делает `.to(device)` над собранным батчем (§3, п.6). Это единственная точка, знающая про CPU/GPU.

**Модель как первая в реестре — НАПОЛНЕНИЕ, не выбор архитектуры (§1, §9).** `statpool_mlp` (`models/statpool.py`): покадровый Linear+ReLU → масочный statistics-pooling (mean+std) по времени → MLP → **один логит** на пример (spoof=1; `BCEWithLogitsLoss`). Лёгкая, C-агностичная, time-агностичная. Смысл Стадии 2 — не «правильная сеть», а замкнутый агностичный цикл; следующая архитектура добавляется рядом новым классом + `@register_model`, цикл не меняется.

---

## 7. Конвенции и правила гигиены `[РЕШЕНО]`

### Память
- **Никогда** не материализовать весь датасет/все признаки в один объект. Всегда потоково / лениво с диска.
- **Хранение — per-file `.npy`** (§6.5): грузится один маленький файл целиком, memmap/HDF5 не нужны. *(Оговорка: шардирование/HDF5 понадобились бы только при очень большом числе файлов или тесном диске. При текущих объёмах (≤30 ГБ данных, ~60 ГБ свободно) — **не требуется**; см. §9. Исключение — SSL-эмбеддинги, которые могут быть крупнее аудио: если дойдёт до их кэширования, прикинуть объём заранее.)*
- **`Dataset.__init__` держит только манифест** (метаданные), не данные — иначе каждый воркер `DataLoader` дублирует это в памяти. Воркеров мало (0–2). *(Реализовано: `dataset.py` хранит только id/метки как numpy-массивы, не `DataFrame`.)*
- **Маленький батч из-за памяти → накопление градиента** (gradient accumulation) для большего *эффективного* батча без роста памяти. *(Включать только при реальном упоре в размер батча — сейчас не реализовано, §9.)*
- **Контроль памяти:** лёгкое логирование RSS (`psutil`) на ключевых шагах, чтобы ловить рост заранее.
- **dtype:** признаки в `float32`, не `float64`. На CPU `float16` не ускоряет и плохо поддержан — оставаться в `float32`.
- **`DataLoader`:** `num_workers` подбирать осторожно — на CPU обучение и загрузка конкурируют за ядра. Значение — параметр конфига (§2).

### Фикс-длина входа и аугментация (Стадия 2) `[РЕШЕНО]`
Каждый пример приводится к общей `t_fixed` (кроп длинного / **повтор-паддинг**
короткого — повтор, а не нули, чтобы кадры оставались «настоящими»). Это собирает
батч в один тензор и держит память батча предсказуемой (§3). Кроп длинного файла:
- **обучение** — окно берётся **случайно** (лёгкая аугментация: за эпохи модель видит разные фрагменты длинных записей → лучше обобщение; доменный стандарт анти-спуфинга);
- **валидация/оценка** — окно **детерминированно** с начала.

Смещение берётся из torch-ГСЧ, который `DataLoader` сеет по воркерам и эпохам →
прогон **воспроизводим от общего seed** (проверено: два прогона от одного seed дают
идентичные метрики), но окно закономерно меняется от эпохи к эпохе.

### Нормализация входа — отложенный хук `[ОТКРЫТО]`
По докстрингу `features.py` нормализацию признаков (если нужна) делает **Стадия 2**,
а не кэш (кэш хранит сырые `[T, C]` для воспроизводимости). Сейчас слот **пуст** —
модель ест сырые LFCC/logmel (`statpool_mlp` частично компенсирует mean/std-пулингом
по времени). Глобальная/поканальная стандартизация — на фазу экспериментов с
признаками/моделью; помечено, чтобы не потерялось (§9).

### Возобновляемость после падения `[РЕШЕНО]`
Требование: стадия, упавшая на N%, при рестарте продолжает с невыполненного. Механизм **разный** для пакетных стадий и для обучения:

- **Стадии 1 и 3 (пакет по файлам) — паттерн «пропустить готовое».**
  - Детерминированный путь выхода на каждый вход (id файла + хэш конфига признаков).
  - Перед обработкой: если выход уже есть → пропустить.
  - **Атомарная запись (критично!):** писать во временный `*.tmp`, затем `os.replace(tmp, final)` (атомарно в пределах одной ФС). Иначе падение *посреди записи* оставит битый файл, ошибочно засчитанный как готовый. Без атомарности проверка «существует → пропустить» небезопасна.
- **Стадия 2 (обучение) — паттерн «чекпойнт».** «Пропустить готовое» неприменимо. *(Реализовано в `train.py`.)*
  - Периодически (каждые `ckpt_every` эпох) сохранять чекпойнт: `state_dict` модели + состояние оптимизатора + эпоха (+ метаданные прогона и `best_eer`).
  - При старте: есть свежий чекпойнт → загрузить и продолжить.
  - Та же атомарная запись (tmp + `os.replace`); держать последние `keep_last` чекпойнтов (ротация), чтобы не остаться с единственным битым. **`keep_last` клампится к ≥ 1** — без чекпойнта нет resume.
  - Лог метрик — в режиме дописывания; при возобновлении **дедуплицировать по эпохе** (строки эпох > эпохи чекпойнта отбрасываются).
  - *Опционально:* для бит-в-бит идентичного возобновления сохранять и состояния ГСЧ (`torch`/`numpy`/`random`). Для *валидного* прогона это не нужно — **не реализовано** намеренно (§9).

### Прочее
- **Воспроизводимость:** фиксировать seed; все параметры запуска (включая `device`, `batch_size`, `num_workers`) — в конфиг-файле (`experiments/<exp>.yaml`), чтобы прогон можно было повторить и приложить к работе.
- **Ноутбуки тонкие:** логика — в `src/viz/`, ноутбук только импортирует, вызывает, сохраняет. Каждый ноутбук обязан проходить **Restart & Run All** сверху вниз без ошибок.
- **Фигуры для печати:** единый стиль через `src/viz/style.py`; линейные графики (loss, DET, ROC) — в **вектор** (PDF/SVG); спектрограммы/тепловые карты — растр с DPI ≥ 300. Экспорт — в `reports/figures/`.

---

## 8. Доменные заметки (специфика анти-спуфинга)

- **Основная метрика — EER** (Equal Error Rate); в литературе проекта она сквозная. Дополнительно встречается t-DCF. *(Общая утилита EER — `src/utils/metrics.py`: ROC-интерполяция точки FPR=FNR; используется val-EER на Стадии 2 и итоговым EER на Стадии 3, чтобы числа были сопоставимы.)*
- **Для графика результатов** стандарт — **DET-кривая** (detection error tradeoff), по которой читается EER; плюс матрица ошибок. *(Строит Стадия 3 / ноутбук результатов, `[НЕ НАЧАТО]`.)*
- **Разбивка EER по отдельным атакам** ценится комиссией: агрегированная метрика скрывает, на каких атаках метод проседает. В нескольких работах обзора это отмечено как слабость — значит, такая разбивка = лёгкий измеримый вклад.
- **Кандидаты признаков из обзора** (контекст, не финальный выбор): ручные кепстральные (LFCC, MFCC, CQCC), мел-спектрограммы под CNN, просодика (F0/энергия/длительность), SSL-эмбеддинги (wav2vec2/HuBERT/WavLM — тяжёлые, на CPU очень медленные).
- **Утечка диктора между сплитами** — известный источник завышенных метрик: если записи одного диктора попадают и в `train`, и в `test`, метод может опознавать диктора, а не подлинность речи. Поэтому при назначении сплита деление **по диктору** предпочтительно (§6.3). Для датасетов без `speaker_id` это ограничение, которое надо **честно оговаривать** в работе.
- **Кросс-датасетная постановка** (учить на одном, тестить на другом) — известная болевая точка: метрики там заметно проседают. Каркас (колонка `dataset_id`, склейка манифестов) поддерживает её бесплатно. *Оценка, не решение:* «оценка переносимости лёгкого метода между датасетами» — потенциально защитимый ракурс новизны; не фиксируется сейчас, но и не закрыт архитектурой.

---

## 9. Статус открытых вопросов

**Решено:**
1. **Память текущей машины = ≤5 ГБ** → `[РЕШЕНО]` (§2, §3, §7), но переформулировано как *свойство машины*, а не аксиома: `device`/`batch_size`/`num_workers` — параметры конфига, проект портируем на GPU.
2. **Место на диске** → `[РЕШЕНО]`: данных ≤30 ГБ при ~60 ГБ свободного места. Дефолт хранения — per-file `.npy`; шардирование/HDF5 **не требуются** (оставлены как строка-оговорка в §7).
3. **Датасеты — множество ОТКРЫТО** → `[РЕШЕНО]`: слой адаптеров `src/data/sources/` с контрактом (§6.2), каноническая схема (§6.1) под валидатор, правило не-ветвления (§3), глобальный ключ (`dataset_id`, `utt_id`).
4. **Признаки подбираются экспериментально** → `[РЕШЕНО как требование]`: `features.py` — реестр; кэш ключуется по конфигу признаков; форма `[T, C]` (§6.6).
5. **Архитектуры подбираются экспериментально** → `[РЕШЕНО]`: `models/` — реестр, цикл обучения модель-агностичен через контракт §6.6. **Модули реализованы (Стадия 2, v11):** реестр (`base.py`) + baseline-НАПОЛНЕНИЕ `statpool_mlp` (`statpool.py`). Выбор конкретной архитектуры — фаза экспериментов (§1), не решается сейчас.
6. **Быстрая итерация на подвыборках** → `[РЕШЕНО]`: детерминированные стратифицированные подвыборки (§6.4, `subsets.py`); итоговый EER — на полном тесте.
7. **Стадия 2 (обучение)** → `[РЕШЕНО]` (v11): модель-агностичный `train.py`, ленивый `dataset.py`, общий EER (`metrics.py`), YAML-раннер, чекпойнты/возобновляемость, pre-flight сверка `C`. Запуск — §4.

**Сознательно отложено (наполнение / фаза экспериментов, не архитектура):**
- **Нормализация входа на Стадии 2** — слот есть (§6.6/§7), кода нет. Стандартизация признаков — когда пойдут содержательные эксперименты.
- **LR-scheduler** — не заведён: при постоянном `lr` лишний. Добавится строкой при надобности.
- **Реальное маскирование переменной длины** — интерфейс (`lengths`) сохранён, но сейчас инертен (все примеры длины `t_fixed`). Включается сменой коллатора без правки моделей.
- **Выбор настоящей архитектуры** — `statpool_mlp` это наполнение (§1); менять его сейчас было бы ошибкой (архитектура выбирается по обзору/экспериментам).
- **Полные прогоны обучения** — отложены до завершения проекта; режим по умолчанию — smoke на подвыборках (§6.4).
- **Продвинутые приёмы обучения** — `WeightedRandomSampler`, gradient accumulation, mixed precision, бит-в-бит возобновление ГСЧ, шардирование/HDF5 — на CPU/скелете не нужны; по надобности (§7).
- **Раннер Стадии 1 по subset-манифесту** («smoke-кэш ровно под smoke-обучение» одной командой) — обсуждалось, не реализовано; сейчас охват согласуется полным `lfcc`-кэшем (§4, «Запуск Стадии 2»).

**Остаётся `[ОТКРЫТО]`:**
- **Дефолтная политика назначения сплита** для датасетов без родного деления (§6.3). Заложено допущение «по диктору, если есть `speaker_id`, иначе случайное с seed + пометка об ограничении» — но дефолт **не подтверждён**. Нужно решение.
- **Стадия 3 (оценка)** — `[НЕ НАЧАТО]`: `evaluate.py`, выгрузка скоров `.parquet`, DET/ROC, разбивка EER по атакам, бутстрэп-CI. Дефолт загрузки весов — `best.pt` (§6.5).
- *На будущее, не срочно:* в метаданных 2021 есть `phase` (progress/eval/hidden) и условия (codec/vocoder/…). Если понадобится считать EER на конкретном подмножестве или разбивать по условиям — добавить нужное отдельной опциональной колонкой в манифест. Пока не требуется.
- Конкретные датасеты / признаки / модели выбираются по ходу экспериментов — это **наполнение**, не архитектура.

---

## Приложение А. Текущие используемые датасеты `[наполнение, не архитектура]`

Инвентарь данных первого эксперимента — чтобы в новом чате было видно, с чем идёт работа. Метки по §6.1: spoof = 1, bonafide = 0. На каждый датасет — один адаптер в `src/data/sources/` (§6.2).

| `dataset_id` | Источник (архив) | Роль | Родной сплит → канон | Ключи/метки | `speaker_id` | `attack_type` |
|---|---|---|---|---|---|---|
| `asvspoof2019_la` | `LA.zip` (ASVspoof 2019 LA) | train + dev (eval тоже есть) | `train/dev/eval` → `train/dev/test` | внутри архива (CM-протоколы) | есть | есть (`A01–A19`) |
| `asvspoof2021_la` | `ASVspoof2021_LA_eval` + `LA-keys-full` | test (аудио полное) | `eval` → `test` | `LA-keys-full/keys/LA/CM/trial_metadata.txt` | есть | есть (`A07–A19`) |
| `asvspoof2021_df` | `ASVspoof2021_DF_eval_part00` (1 из 3) + `DF-keys-full` | test (аудио частично) | `eval` → `test` | `DF-keys-full/keys/DF/CM/trial_metadata.txt` | есть (неоднородный) | есть (`A07–A19` + `TaskN-teamXX`) |
| `for-2seconds` | `for-2sec.tar.gz` (FoR, 2 сек) | test (кросс-датасет) | `training/validation/testing` → `train/dev/test` | нет; метка = папка (`real`/`fake`) | нет | нет |

**Ключевое:**
- Наборы 2021 — только eval; train/dev для них берётся из 2019 LA. Отсюда естественная кросс-датасетная постановка (§8).
- Метки 2021 лежат в `trial_metadata.txt` внутри `*-keys-full`, а не в аудио-архиве (файл `*.cm.eval.trl.txt` рядом с аудио — только список имён).
- DF скачан намеренно частично (`part00` из трёх) — это решение, не недокачка, докачивать не нужно. Адаптер индексирует присутствующее аудио, метку берёт из полных ключей; EER по DF, соответственно, считается на подмножестве — так и указывать в работе.

**Формат `trial_metadata` (2021), пробел-разделитель; метка — поле 6, аудио — `flac/<utt_id>.flac`:**
- *LA, 8 полей:* `speaker(1) utt_id(2) codec(3) transmission(4) attack(5) label(6) trim(7) phase(8)`. attack ∈ {A07…A19}, у bonafide = `bonafide`. phase ∈ {progress, eval, hidden}.
- *DF, 13 полей:* `speaker(1) utt_id(2) codec(3) source(4) attack(5) label(6) trim(7) phase(8) vocoder(9) + vcc-мета(10–13)`. attack ∈ {A07…A19} или `TaskN-teamXX`, у bonafide = `-`. Поля 10–13 — только для VCC, иначе `-`. `vocoder` — удобная ось для разбивки EER по DF.
- Подлинные строки размечены по-разному (LA: `bonafide`, DF: `-`) → у обоих `attack_type = NULL`; это нормализует адаптер.

Сверить по файлам перед адаптерами: формат 2019 LA CM-протокола и точные имена папок FoR.

**Фактическая раскладка `raw/` (`tree -d`)**:
```text
raw
├── asvspoof2019_la
│   ├── ASVspoof2019_LA_asv_protocols
│   ├── ASVspoof2019_LA_asv_scores
│   ├── ASVspoof2019_LA_cm_protocols
│   ├── ASVspoof2019_LA_dev
│   │   └── flac
│   ├── ASVspoof2019_LA_eval
│   │   └── flac
│   └── ASVspoof2019_LA_train
│       └── flac
├── asvspoof2021_df
│   └── flac
├── asvspoof2021_la
│   └── flac
├── DF-keys-full
│   └── keys
│       └── DF
│           └── CM
│               ├── CQCC-GMM
│               ├── LFCC-GMM
│               ├── LFCC-LCNN
│               └── RawNet2
├── for-2seconds
│   ├── testing
│   │   ├── fake
│   │   └── real
│   ├── training
│   │   ├── fake
│   │   └── real
│   └── validation
│       ├── fake
│       └── real
└── LA-keys-full
    └── keys
        └── LA
            ├── ASV
            │   └── ASVTorch_Kaldi
            └── CM
                ├── CQCC-GMM
                ├── LFCC-GMM
                ├── LFCC-LCNN
                └── RawNet2
```
Существенное: ключи 2021 (`LA-keys-full/`, `DF-keys-full/`) лежат **соседями** папок аудио, а не внутри `raw/asvspoof2021_*/` — адаптеры 2021 резолвят их через корень `raw/` (константа `_KEYS_RELPATH`). 



---

## 10. Changelog

- **v11 (текущая):**
  - **Стадия 2 (обучение) реализована и проверена end-to-end.** Новые модули:
    `src/train.py` (модель-агностичный цикл + тонкий YAML-раннер `python -m src.train
    --config …`), `src/data/dataset.py` (ленивый `FeatureCacheDataset` поверх кэша +
    коллатор; фикс-длина `t_fixed` кропом/повтор-паддингом, **рандом-кроп на
    обучении** / детерминированное окно на валидации), `src/data/subsets.py`
    (детерминированные стратифицированные подвыборки, §6.4; инвариант «стабильная
    сортировка → seed»), `src/models/` (реестр `base.py` + baseline `statpool.py` =
    `statpool_mlp`), `src/utils/metrics.py` (общий EER ROC-интерполяцией,
    переиспользуется Стадией 3).
  - **Контракт §6.6 замкнут:** модель объявляет `C` (`in_channels`); **pre-flight** в
    `train.py` сверяет `C` кэша (`_spec.json`) с `C` модели → рассинхрон/отсутствие
    кэша ловятся до обучения. Форма батча `[B, t_fixed, C]` + `lengths` (маска пока
    тривиальна — фикс-длина; интерфейс сохранён под переменную длину).
  - **Возобновляемость (§7):** чекпойнты (модель + оптимизатор + эпоха) атомарной
    записью (tmp + `os.replace`), ротация последних `keep_last` (**клампится к ≥ 1**),
    подхват свежего при рестарте; `metrics.jsonl` в режиме дописывания с дедупом по
    эпохе. Отдельно `best.pt` (мин. val-EER) и `final.pt`.
  - **Дисбаланс классов:** `pos_weight = N_neg/N_pos` (auto) в `BCEWithLogitsLoss`,
    считается по фактическим (в т.ч. подвыборочным) train-меткам; spoof = 1 (§6.1).
  - **Зависимость:** `pyyaml` — обязательная (чтение `experiments/<exp>.yaml`, §2).
    Пример smoke-конфига — `experiments/smoke_lfcc_statpool.yaml`.
  - **Валидация:** прогоны обучения / возобновления / pre-flight / ротации /
    детерминизма (два прогона от одного seed → идентичные метрики) на синтетическом
    кэше; юнит-тесты `subsets`/`metrics`. Smoke на реальном `lfcc`-кэше (train/dev
    2019 LA) отработал: цикл учится, val-EER считается по эпохам.
  - **`models/` снят статус `[НЕ НАЧАТО]`** (§5, §9). `statpool_mlp` — НАПОЛНЕНИЕ, не
    выбор архитектуры (§1). Сознательно отложено (§7, §9): нормализация входа,
    LR-scheduler, реальное маскирование переменной длины, выбор архитектуры, полные
    прогоны.
- **v10:**
  - **Стадия 1 (`features.py`) реализована и проверена end-to-end.** Реестр экстракторов (зеркало `sources/`) + `lfcc` (C=60) / `logmel` (C=80); контракт формы `[T, C]` float32 с проверкой на каждом выходе (§6.6). Per-file `.npy` кэш с ключом (`dataset_id`, `utt_id`) + sha1 конфига признаков; папка кэша самоописательна (`_spec.json`, `_failures.csv`). Возобновляемость «пропустить готовое» + атомарная запись через новую `utils/io.atomic_write_npy`. Тонкий раннер (`-m src.data.features`, флаги `datasets/-f/--all/--list/--limit/--root`), устойчивость к битым файлам; руководство по запуску — в §4.
  - **Ловушка окружения (§2):** libsndfile 1.2.2 не декодирует часть валидных FLAC ASVspoof (баг его FLAC-декодера; заголовок при этом читается) → зафиксирован `soundfile==0.12.1` (libsndfile 1.2.0, чистый). `features.py` имеет ffmpeg-фолбэк как страховку.
  - **Валидация:** полные прогоны `lfcc` по всем 4 датасетам без ошибок; `logmel` — smoke (полный отложен по диску, §7). Санити `.npy` подтвердил корректный LFCC (форма, разброс по C0). `config.py` не менялся; точка входа под будущий YAML-оверрайд — `get_extractor(name, **overrides)`.
- **v9:**
  - Реализована Стадия 0. Написаны `config.py`, `schema.py` (+ валидатор), реестр адаптеров (`sources/base.py`), 4 адаптера (`asvspoof2019_la`, `asvspoof2021_la`, `asvspoof2021_df`, `for_2seconds`), оркестратор `manifest.py`, `utils/io.py` (атомарная запись). Зафиксировано: `pyarrow` — обязательная зависимость (движок parquet); фактическая раскладка `raw/` в Приложении А; ключи 2021 — соседи папок аудио. Условия оценки 2021 (phase/vocoder/transmission) сознательно не заносятся в манифест (§9, «по надобности»). Добавлен тонкий раннер Стадии 0 (`python -m src.data.manifest`, флаги `--all/--list/--root`) — независимый запуск стадии, не глобальный `main.py`. Адаптер 2019 LA индексирует по CM-протоколу (ASV-enrollment из dev/eval исключён).
- **v8:**
  - Снижена избыточная строгость тона (по запросу — не переусложнять). Приложение А переписано короче: убраны тройные оговорки и защитные теги, сохранена вся полезная справка (таблица датасетов, формат `trial_metadata`, ловушка с разметкой bonafide). Вопрос про `phase`/условия в §9 понижен из «открытой правки контракта §6.1» до необязательной заметки «добавить колонку по надобности». Архитектура §1–§8 не тронута.
- **v7:**
  - **Приложение А:** зафиксирован *проверенный построчно* формат `trial_metadata` наборов 2021 (LA — 8 полей, DF — 13 полей): позиции `label`/`attack`/`phase`/`speaker_id`/условий и их словари значений. Подтверждено: у обоих 2021 есть `speaker_id`; метки — только в keys, не в аудио-архиве; в LA у bonafide поле-атака = `bonafide`, в DF = `-` (ловушка адаптера).
  - **§9:** новый открытый вопрос — как протаскивать `phase` (progress/eval/hidden) и условия оценки (codec/transmission/vocoder/source) через манифест; в §6.1 есть слот только под `codec`. Влияет на сопоставимость EER (критерий №2) и разбивку EER (§8). Предложены варианты A/B/C.
- **v6:**
  - Добавлено **Приложение А** — инвентарь датасетов первого эксперимента (`asvspoof2019_la`, `asvspoof2021_la`, `asvspoof2021_df` [частично], `for-2seconds`): роли, сплиты, ключи, наличие `speaker_id`/`attack_type`. Помечено как *наполнение* (§9), не архитектура.
  - Зафиксировано **намеренно частичное** скачивание DF (только `part00`) как решение пользователя и его следствие: EER на DF отчитывается как EER на подмножестве.
- **v5:**
  - §3 переформулирован: ядро архитектуры разведено на *долговечные, машинно-независимые* принципы (кэш признаков, возобновляемость, развязанность, портируемость) и *свойства текущей машины* (потолок 5 ГБ, CPU). «RAM ~ размер батча» — принцип; «5 ГБ» — профиль машины, не аксиома.
  - Введена **портируемость на GPU** без абстракций: `device`/`batch_size`/`num_workers` — параметры конфига (§2, §3, §7); единственная точка `.to(device)` — коллатор/цикл (§6.6).
  - §1: разведены «на чём обучается метод» (переменная) и «вес метода» (вклад новизны); отмечен дисциплинарный риск тяжёлого SSL при появлении GPU.
  - Добавлен **контракт признак↔модель** (§6.6): форма `[T, C]` `float32`, коллация с маской длин, объявление входа моделью — третий проверяемый интерфейс агностичности.
  - **Упрощения:** убраны шардирование / HDF5 / memmap (дефолт per-file `.npy` + одна строка-оговорка, §7); правило не-ветвления сжато до одного правила + «code smell»; §10 из повторного вывода решений сокращён до changelog; бит-в-бит ГСЧ понижен до опциональной строки; grad-accumulation помечен «только при упоре в батч».
  - **Место на диске** закрыто (§9).
- **v1–v4 (сжато):** пайплайн разнесён на стадии 0–3 с кэшем на диск; единая точка входа отклонена (стадии независимы); графики — 4 тонких ноутбука, логика в `src/viz/`; зафиксированы каноническая схема манифеста + валидатор + конвенция NULL; введён слой адаптеров датасетов; глобальный ключ (`dataset_id`, `utt_id`); политика сплитов (родной vs назначенный, дефолт открыт); детерминированные стратифицированные подвыборки; реестры признаков и моделей; механизм возобновляемости (пропустить-готовое + атомарная запись; чекпойнты с ротацией).